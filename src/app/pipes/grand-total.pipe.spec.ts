import { GrandTotalPipe } from './grand-total.pipe';

describe('GrandTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new GrandTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
