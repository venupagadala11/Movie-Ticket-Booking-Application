import { TicketsCostPipe } from './tickets-cost.pipe';

describe('TicketsCostPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketsCostPipe();
    expect(pipe).toBeTruthy();
  });
});
